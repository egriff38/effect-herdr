/**
 * effect-herdr — typed Effect-TS SDK for the herdr terminal multiplexer.
 *
 * This is the entrypoint. Nothing lives here yet — the design (RpcGroup
 * protocol layer + HerdrSession Context.Service ergonomic layer) is being
 * grilled in a separate session before implementation lands.
 *
 * See `docs/design.md` (to be authored during the grilling session) for the
 * full proposal.
 */

export {}
